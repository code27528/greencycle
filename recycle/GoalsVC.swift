//
//  GoalsVC.swift
//  recycle
//
//  Created by Sohom Dutta on 7/20/20.
//  Copyright © 2020 Sohom Dutta. All rights reserved.
//

import UIKit

class GoalsVC: UIViewController {

    /* @IBOutlet weak var addNumber: UILabel!
    
    @IBOutlet weak var totalCount: UILabel!*/
    
    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
    }
    
    /*@IBAction func addOne(_ sender: Any) {
        if let apple = addNumber.text{
            if let number = Int(apple){
                let plus = number + 1
                addNumber.text = "\(plus)"
            }
        }
    }
    @IBAction func minusOne(_ sender: Any) {
        if let apple = addNumber.text{
            if let number = Int(apple){
                let minus = number - 1
                addNumber.text = "\(minus)"
            }
        }
    }
    
    @IBAction func minusBigOne(_ sender: Any) {
        if let apple = totalCount.text{
            if let number = Int(apple){
                let minus = number - 1
                totalCount.text = "\(minus)"
            }
        }
    }
    @IBAction func addGoal(_ sender: Any) {
        if let apple = addNumber.text{
            if let number = Int(apple){
                if let banana = totalCount.text{
                    if let number2 = Int(banana){
                        let count = number + number2
                        totalCount.text = "\(count)"
                    }
                }
            }
        }
    }
    @IBAction func clearGoal(_ sender: Any) {
        totalCount.text = "0"
    }*/
    
    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
